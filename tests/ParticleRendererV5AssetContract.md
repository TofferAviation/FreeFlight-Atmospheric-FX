# Renderer Foundation v5 asset contract

The v5 proof package must contain a particle-only OBJ, a native X-Plane ribbon `.pss`, and one RGBA cloud texture. It must not contain OBJ card triangles or a `TEXTURE_LIT` card material.
